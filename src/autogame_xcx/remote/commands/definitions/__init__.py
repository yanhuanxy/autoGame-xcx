"""具体指令实现。

每个指令一个文件，便于增删（不影响其他指令）。
当前阶段（2.3）实现 6 个：
    help          — 查看可用指令
    list_templates — 列出可用模板
    run_template  — 触发模板执行
    status        — 查询当前执行状态（占位，2.4 阶段完整）
    stop          — 停止当前任务（占位，2.4 阶段完整）
    report        — 查询最近执行报告
"""
